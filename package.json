{
  "name": "determinist-search-key-for-integration-playgrounds",
  "version": "0.1.0",
  "description": "Creates the deterministic search-api key in the integrations instance",
  "main": "index.js",
  "scripts": {
    "create-key": "node ./index.js",
    "lint": "eslint --ext .js .",
    "lint:fix": "eslint --ext .js . --fix",
    "test": "jest --runInBand --verbose --coverage",
    "test:watch": "jest --runInBand --verbose --coverage --watch"
  },
  "files": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/meilisearch/integration_deterministic_search_api_key"
  },
  "keywords": [
    "meilisearch",
    "search"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/meilisearch/integration_deterministic_search_api_key"
  },
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "jest": "^28.1.2",
    "prettier": "^2.3.2"
  },
  "dependencies": {
    "@types/jest": "^28.1.4",
    "@types/node": "^18.14.2",
    "dotenv": "^16.0.1",
    "meilisearch": "^0.27.0"
  },
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": ["./src/**"],
    "coverageThreshold": {
      "global": {
        "lines": 90
      }
    }
  }
}
